#include <iostream>
using namespace std;
int main(){
    int one = 0, two = 0, three = 0;
    cin >> one >> two >> three;
    cout << "The 1-3-sum is " << 91 + one + (two * 3) + three << endl;
    return 0;
}